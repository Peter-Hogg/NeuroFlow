# Generated tables

Run `uv run python publication/generate_tables.py` to regenerate Markdown and
CSV tables from benchmark schema records. Files with malformed or historical
records are reported and excluded rather than silently mixed with current
evidence.
